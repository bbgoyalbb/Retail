# Required: Backend API base URL (no trailing slash, no /api suffix)
REACT_APP_BACKEND_URL=http://127.0.0.1:8001

# Optional: Set to "true" to auto-seed the database from Excel on first load.
# Only enable this in development when an Excel seed file is available.
REACT_APP_ENABLE_SEED=false